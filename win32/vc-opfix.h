#pragma once

// Visual Studio does not support alternative operators, so
// these definitions for them are force-#included across the project.

#define and &&
#define or ||
#define not !
#define xor ^
